export enum EScreen {
    LANG_SELECT = 'lang_select',
    MAIN_MENU = 'main.menu',
    WORDS = 'main.words',
    START_MENU = 'start.menu',
}

export enum EScreenAction {
    EDIT,
    SEND,
}

export enum EBotLang {
    EN = 'en',
    RU = 'ru',
}
